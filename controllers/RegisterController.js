var app = angular.module('Register', []);

app.controller('RegisterController', ['$scope',
    function($scope) {

        var init = function() {
            
            
        }

        init();
    }
]);

